import { PacketStub } from "./PacketStub";
export declare const sentenceId: "ZDA";
export declare const sentenceName = "UTC, day, month, year, and local time zone";
export interface ZDAPacket extends PacketStub<typeof sentenceId> {
    datetime: Date;
    localZoneHours: number;
    localZoneMinutes: number;
}
export declare function decodeSentence(stub: PacketStub, fields: string[]): ZDAPacket;
