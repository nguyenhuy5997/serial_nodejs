import { PacketStub } from "./PacketStub";
export declare const sentenceId: "?";
export interface UnknownPacket extends PacketStub<typeof sentenceId> {
    dataFields: string[];
    originalPacketId: string;
}
export declare function decodeSentence(stub: PacketStub, fields: string[]): UnknownPacket;
