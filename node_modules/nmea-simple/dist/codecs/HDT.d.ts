import { PacketStub } from "./PacketStub";
export declare const sentenceId: "HDT";
export declare const sentenceName = "Heading - true";
export interface HDTPacket extends PacketStub<typeof sentenceId> {
    heading: number;
}
export declare function decodeSentence(stub: PacketStub, fields: string[]): HDTPacket;
export declare function encodePacket(packet: HDTPacket, talker: string): string;
