import { PacketStub } from "./PacketStub";
export declare const sentenceId: "DBT";
export declare const sentenceName = "Depth below transducer";
export interface DBTPacket extends PacketStub<typeof sentenceId> {
    depthFeet: number;
    depthMeters: number;
    depthFathoms: number;
}
export declare function decodeSentence(stub: PacketStub, fields: string[]): DBTPacket;
export declare function encodePacket(packet: DBTPacket, talker: string): string;
