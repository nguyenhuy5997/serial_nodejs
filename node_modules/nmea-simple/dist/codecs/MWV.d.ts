import { PacketStub } from "./PacketStub";
export declare const sentenceId: "MWV";
export declare const sentenceName = "Wind speed and angle";
export interface MWVPacket extends PacketStub<typeof sentenceId> {
    windAngle: number;
    reference: "relative" | "true";
    speed: number;
    units: "K" | "M" | "N";
    status: "valid" | "invalid";
}
export declare function decodeSentence(stub: PacketStub, fields: string[]): MWVPacket;
export declare function encodePacket(packet: MWVPacket, talker: string): string;
